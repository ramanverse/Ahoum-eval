# =============================================================================
# requirements-docker.txt — Lean deps for Docker image (CPU-only, no CUDA)
# =============================================================================
# Used exclusively in Dockerfile. Installs torch CPU-only (~220MB vs 870MB).
# The main requirements.txt is for local dev and may include heavier deps.
# =============================================================================

# --- Torch CPU-only (install BEFORE other deps via the extra index) ----------
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# --- Core ML / NLP -----------------------------------------------------------
transformers>=4.36.2
accelerate>=0.25.0
sentencepiece>=0.1.99
sentence-transformers>=2.3.1

# --- NLP Tooling -------------------------------------------------------------
spacy>=3.7.2
nltk>=3.8.1
textblob>=0.17.1

# --- Data Processing ---------------------------------------------------------
pandas>=2.1.3
numpy>=1.24.3
pyarrow>=14.0.1

# --- API (FastAPI + server) --------------------------------------------------
fastapi>=0.109.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.3
python-multipart>=0.0.6

# --- Streamlit Dashboard -----------------------------------------------------
streamlit>=1.28.1
plotly>=5.18.0
altair>=5.2.0

# --- Utilities ---------------------------------------------------------------
pyyaml>=6.0.1
tqdm>=4.66.1
loguru>=0.7.2
rich>=13.7.0
click>=8.1.7
